<h1><%= @book.title %> (<%= @book.year %>)</h1>
<h2><%= link_to @author.name, @author %></h2>
<% if @book.has_quote? %>
    <br/>
    <p>This book opens with the epigraph:
    <h2>"<%= link_to @quote.body, @quote %>"</h2>
    <p>— <%= link_to @quote_author.name, @quote_author %>.
<% end %>
