<h1>Authors</h1>
<p>The finest authors in town. They have quoted, and they have been quoted.</p>

  <% @initials.each do |initial, author| %>
    <h3><%= initial %></h3>
    <ul>
      <% author.each do |a| %>
        <li>
          <%= link_to "#{a.first_name} #{a.last_name}", a %>
        </li>
      <% end %>
    </ul>
  <% end %>